# Custom Personality AI chatbot
Custom DeepSeek-Based Chatbot (HTML Only)

A lightweight, frontend-only chatbot powered by DeepSeek logic, built entirely using a single HTML file. No backend. No frameworks. Just vibes.

🚀 Features

Pure HTML + CSS + JavaScript

No backend / server required

Simple, clean chat UI

Fast responses with minimal overhead

Easy to customize prompts, UI, and logic

Works offline (if logic is local)

🧠 How It Works

The chatbot logic is implemented directly inside the HTML file using JavaScript.
All UI rendering, input handling, and response generation happen client-side.
